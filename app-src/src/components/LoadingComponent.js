import { Card } from "react-bootstrap";

function LoadingComponent() {
    return(
        <Card>
            <Card.Body>
                <h1>Loading...</h1>
            </Card.Body>
        </Card>
    )
}

export default LoadingComponent;